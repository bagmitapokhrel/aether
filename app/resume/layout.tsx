export const metadata = {
  title: "Resume — Bagmita Pokhrel",
  description: "The straightforward version of what I've done so far.",
};

export default function ResumeLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}